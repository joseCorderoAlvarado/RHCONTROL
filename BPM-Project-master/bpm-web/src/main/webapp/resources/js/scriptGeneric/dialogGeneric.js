function validNewObjectGeneric(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg2').hide();
    } else {
        PF('dlg2').show();
    }
}

function validNewObjectGeneric2(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg3').hide();
    } else {
        PF('dlg3').show();
    }
}

function validNewObjectGeneric3(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg4').hide();
    } else {
        PF('dlg4').show();
    }
}

function validNewObjectGeneric4(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg5').hide();
    } else {
        PF('dlg5').show();
    }
}

function validNewObjectGeneric5(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg6').hide();
    } else {
        PF('dlg6').show();
    }
}

function validNewObjectGeneric6(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg7').hide();
    } else {
        PF('dlg7').show();
    }
}

function validNewObjectGeneric7(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg8').hide();
    } else {
        PF('dlg8').show();
    }
}

function validNewObjectGeneric8(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg9').hide();
    } else {
        PF('dlg9').show();
    }
}

function validNewObjectGeneric9(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg10').hide();
    } else {
        PF('dlg10').show();
    }
}

function validNewObjectGeneric10(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg11').hide();
    } else {
        PF('dlg11').show();
    }
}

function validNewObjectGeneric11(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('dlg12').hide();
    } else {
        PF('dlg12').show();
    }
}

function deleteEventBitacora(xhr, status, args) {
    if (undefined === args.validationFailed || false === args.validationFailed) {
        PF('deleteEventBitacora').hide();
    } else {
        PF('deleteEventBitacora').show();
    }
}
;
